import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { ShoppingCart, Heart, Lock } from 'lucide-react';
import { useAuth } from '@/contexts/AuthContext';
import necklaceImage from '@/assets/necklace-sapphire.jpg';
import ringImage from '@/assets/ring-blue.jpg';
import braceletImage from '@/assets/bracelet-blue.jpg';

const products = [
  {
    id: 1,
    name: 'Colar Safira Azul',
    price: 'R$ 35,00',
    image: necklaceImage,
    description: 'Elegante colar em prata com pendente de safira azul'
  },
  {
    id: 2,
    name: 'Anel Pedra Azul',
    price: 'R$ 35,00',
    image: ringImage,
    description: 'Sofisticado anel em prata com pedra preciosa azul'
  },
  {
    id: 3,
    name: 'Pulseira Pedras Azuis',
    price: 'R$ 35,00',
    image: braceletImage,
    description: 'Delicada pulseira em prata com pedras azuis'
  }
];

const Products = () => {
  const { user } = useAuth();

  return (
    <section id="collections" className="py-20 bg-gradient-to-b from-white to-blue-50/30">
      <div className="container mx-auto px-4">
        <div className="text-center mb-16">
          <h2 className="text-4xl md:text-5xl font-display font-bold text-gradient mb-6 tracking-tight">
            Nossa Coleção
          </h2>
          <p className="text-xl text-gray-600 max-w-2xl mx-auto font-light tracking-wide">
            Peças exclusivas em prata, cuidadosamente selecionadas para realçar sua beleza única
          </p>
          {!user && (
            <div className="mt-6 p-6 bg-blue-50 border border-blue-200 rounded-xl max-w-md mx-auto">
              <div className="flex items-center justify-center mb-3">
                <Lock className="w-6 h-6 text-blue-700 mr-2" />
                <span className="text-blue-800 font-medium">Faça login para ver os preços</span>
              </div>
              <p className="text-blue-600 text-sm">
                Cadastre-se gratuitamente para acessar nossa coleção completa com preços especiais
              </p>
            </div>
          )}
        </div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {products.map((product) => (
            <Card key={product.id} className="card-elegant group cursor-pointer overflow-hidden">
              <div className="relative overflow-hidden">
                <img
                  src={product.image}
                  alt={product.name}
                  className="w-full h-80 object-cover transition-transform duration-500 group-hover:scale-110"
                />
                <div className="absolute top-4 right-4">
                  <Button
                    size="sm"
                    variant="ghost"
                    className="bg-white/80 hover:bg-white text-gray-700 rounded-full p-2"
                  >
                    <Heart className="w-4 h-4" />
                  </Button>
                </div>
              </div>
              
              <CardContent className="p-6">
                <h3 className="text-xl text-elegant font-semibold text-gray-900 mb-2 tracking-wide">
                  {product.name}
                </h3>
                <p className="text-gray-600 mb-4 text-sm">
                  {product.description}
                </p>
                
                <div className="flex items-center justify-between">
                  {user ? (
                    <>
                      <span className="text-2xl font-bold text-blue-900">
                        {product.price}
                      </span>
                      <Button 
                        className="bg-blue-900 hover:bg-blue-800 text-white px-6 py-2 rounded-lg transition-all duration-300 hover:shadow-lg flex items-center gap-2"
                      >
                        <ShoppingCart className="w-4 h-4" />
                        Comprar
                      </Button>
                    </>
                  ) : (
                    <div className="w-full flex items-center justify-center py-3 bg-blue-50 border border-blue-200 rounded-lg">
                      <Lock className="w-4 h-4 text-blue-700 mr-2" />
                      <span className="text-blue-800 font-medium">Login necessário</span>
                    </div>
                  )}
                </div>
              </CardContent>
            </Card>
          ))}
        </div>

        <div className="text-center mt-12">
          <Button 
            size="lg"
            variant="outline"
            className="border-blue-900 text-blue-900 hover:bg-blue-900 hover:text-white px-8 py-3 rounded-xl transition-all duration-300"
          >
            Ver Toda Coleção
          </Button>
        </div>
      </div>
    </section>
  );
};

export default Products;